(ns os-simulator.processor
  (:require [os-simulator.disk :as d]))


(defn spooling
  "spooling event. (1)"
  [event-engine]
  event-engine)

(defn job-scheduling
  "docstring"
  [event-engine]
  event-engine)